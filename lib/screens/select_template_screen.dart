import 'package:flutter/material.dart';

class SelectTemplate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Mis Plantillas')),
      body: Container(
        child: Center(
          child: Text('Selecciona tu plantilla !!!'),
        ),
      ),
    );
  }
}
